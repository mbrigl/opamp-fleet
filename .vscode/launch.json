{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Server",
      "cargo": {
        "args": ["build", "--bin", "server", "--package", "server"],
        "filter": { "name": "server", "kind": "bin" }
      },
      "args": ["--config", "config/server.toml"],
      "cwd": "${workspaceFolder}"
    },
    {
      // The package is `client`, the binary it produces is `opamp-fleet-client` (ADR-0028):
      // `--package` takes the first, `--bin` and the filter take the second.
      "type": "lldb",
      "request": "launch",
      "name": "Client",
      "cargo": {
        "args": ["build", "--bin", "opamp-fleet-client", "--package", "client"],
        "filter": { "name": "opamp-fleet-client", "kind": "bin" }
      },
      "args": ["--config", "config/client.toml"],
      "cwd": "${workspaceFolder}"
    }
  ],
  "compounds": [
    {
      "name": "Server + Client",
      "configurations": ["Server", "Client"],
      "stopAll": true
    }
  ]
}
